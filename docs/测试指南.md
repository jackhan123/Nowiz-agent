# 测试指南

## 🚀 快速开始

### 1. 安装依赖（如果还没安装）
```bash
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

### 3. 打开浏览器
访问：http://localhost:3000

---

## 📋 测试步骤

### 测试 1：左侧需求完善功能

#### 步骤：
1. 在左侧输入框输入简单想法，例如：
   - `我想做一个智能台灯`
   - `我想做一个水晶球`
   - `我想做一个宠物喂食器`

2. 点击"发送"按钮或按 `Enter` 键

3. 观察：
   - ✅ 消息应该出现在对话区域
   - ✅ AI 会回复（可能需要 5-15 秒）
   - ✅ 如果 AI 返回 JSON 格式，会显示结构化的需求结果

4. 继续对话：
   - 可以继续回答 AI 的问题
   - 或者等待 AI 自动完善需求

5. 需求完善后：
   - ✅ 会显示一个蓝色卡片，展示完善后的需求
   - ✅ 出现"复制"和"→ 复制到右侧"按钮

#### 预期结果：
- 左侧对话窗口正常显示消息
- AI 能够理解并完善需求
- 最终显示结构化的需求文档

---

### 测试 2：右侧 BOM 生成功能

#### 方式 A：直接输入简单想法
1. 在右侧输入框输入：
   - `智能台灯`
   - `环境监测器`
   - `桌面时钟`

2. 点击"生成 BOM 清单"按钮

3. 观察：
   - ✅ 按钮显示"生成中..."
   - ✅ 等待 20-60 秒（AI 生成需要时间）
   - ✅ 显示 BOM 清单表格

#### 方式 B：使用左侧完善的需求
1. 先在左侧完成需求完善（参考测试 1）

2. 点击左侧的"→ 复制到右侧"按钮

3. 观察：
   - ✅ 右侧输入框自动填充完善后的需求

4. 点击"生成 BOM 清单"按钮

5. 观察：
   - ✅ 生成更准确的 BOM 清单

#### 预期结果：
- 右侧能够生成完整的 BOM 清单
- 表格显示：分类、零件、型号、数量、单价、小计
- 底部显示总成本、难度、组装时间

---

### 测试 3：一键复制功能

#### 步骤：
1. 在左侧完成需求完善

2. 点击"复制"按钮

3. 打开任意文本编辑器，按 `Cmd+V` (Mac) 或 `Ctrl+V` (Windows)

4. 观察：
   - ✅ 应该粘贴完整的需求文档

5. 点击"→ 复制到右侧"按钮

6. 观察：
   - ✅ 右侧输入框自动填充需求内容

#### 预期结果：
- 复制功能正常工作
- 复制到右侧功能正常工作

---

### 测试 4：响应式布局

#### 步骤：
1. 在桌面浏览器中打开页面

2. 调整浏览器窗口大小：
   - 宽屏（>1024px）：两栏并排显示
   - 窄屏（<1024px）：两栏垂直堆叠

3. 在移动设备或移动端模拟器中测试

#### 预期结果：
- 桌面端：两栏并排显示
- 移动端：两栏垂直堆叠，每个栏占满宽度

---

## ⚠️ 常见问题排查

### 问题 1：API 请求失败
**症状**：显示"API 请求失败"或网络错误

**解决方案**：
1. 检查网络连接
2. 检查 API Key 是否有效
3. 查看浏览器控制台（F12）的错误信息
4. 如果使用环境变量，确保 `.env.local` 文件存在且格式正确

### 问题 2：AI 返回格式错误
**症状**：显示"AI 返回格式错误"或"AI 返回数据不完整"

**解决方案**：
1. 这是正常的，AI 有时会返回非 JSON 格式
2. 左侧：继续对话，AI 会逐步完善
3. 右侧：重试生成，或尝试更简单的输入

### 问题 3：请求超时
**症状**：显示"请求超时"

**解决方案**：
1. AI 生成需要 20-60 秒，请耐心等待
2. 检查网络速度
3. 如果频繁超时，可能是 API 限流，稍后再试

### 问题 4：页面样式异常
**症状**：布局错乱、样式丢失

**解决方案**：
1. 确保 Tailwind CSS 已正确安装
2. 重启开发服务器：`npm run dev`
3. 清除浏览器缓存

### 问题 5：依赖未安装
**症状**：启动时报错，找不到模块

**解决方案**：
```bash
# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

---

## 🔍 调试技巧

### 1. 查看浏览器控制台
- 按 `F12` 打开开发者工具
- 查看 `Console` 标签页的错误信息
- 查看 `Network` 标签页的 API 请求

### 2. 检查 API 响应
在 `Network` 标签页中：
1. 找到对 `dashscope.aliyuncs.com` 的请求
2. 点击查看 `Response` 内容
3. 检查返回的 JSON 格式是否正确

### 3. 测试 API Key
如果 API Key 无效，可以在浏览器控制台运行：
```javascript
fetch('https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_DASHSCOPE_KEY',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    model: 'qwen-plus',
    messages: [{ role: 'user', content: '你好' }]
  })
}).then(r => r.json()).then(console.log)
```

---

## ✅ 测试检查清单

- [ ] 左侧可以发送消息
- [ ] 左侧 AI 能够回复
- [ ] 左侧能够完善需求并显示结果
- [ ] 左侧"复制"按钮正常工作
- [ ] 左侧"→ 复制到右侧"按钮正常工作
- [ ] 右侧可以输入内容
- [ ] 右侧能够生成 BOM 清单
- [ ] 右侧 BOM 表格显示完整
- [ ] 移动端布局正常
- [ ] 桌面端两栏布局正常

---

## 📝 测试用例示例

### 用例 1：完整流程测试
1. 左侧输入：`我想做一个智能台灯`
2. 等待 AI 完善需求
3. 点击"→ 复制到右侧"
4. 右侧点击"生成 BOM 清单"
5. 验证 BOM 清单是否包含台灯相关元件

### 用例 2：快速测试
1. 右侧直接输入：`智能台灯`
2. 点击"生成 BOM 清单"
3. 验证是否生成基本 BOM

### 用例 3：边界测试
1. 左侧输入空内容 → 应该禁用发送按钮
2. 右侧输入空内容 → 应该禁用生成按钮
3. 网络断开 → 应该显示错误提示

---

## 🎯 成功标准

测试通过的标准：
- ✅ 所有功能按钮都能正常响应
- ✅ AI 能够理解和回复
- ✅ 能够生成有效的需求文档和 BOM 清单
- ✅ 布局在不同屏幕尺寸下正常显示
- ✅ 没有明显的错误或崩溃

祝你测试顺利！🚀
