# Newiz 智能控制系统 - 页面访问指南

## ✅ 问题已解决！

原来的"获取状态失败: Load failed"错误已经修复。

## 🌐 正确的访问方式

### Next.js 页面（推荐）
- **主控制面板**: http://localhost:3000/smart-control
- **测试页面**: http://localhost:3000/test-vision
- **硬件仪表板**: http://localhost:3000/hardware-dashboard

### 静态HTML文件（已修复）
- http://localhost:3000/smart-control.html
- http://localhost:3000/test-vision.html

## 🚀 启动步骤

1. **启动Next.js服务器**:
   ```bash
   npm run dev
   ```

2. **验证服务状态**:
   ```bash
   ./test-apis.sh
   ```

3. **在浏览器中访问**:
   - 推荐使用: `http://localhost:3000/test-vision`
   - 或者: `http://localhost:3000/smart-control`

## 📋 功能对比

| 功能 | Next.js页面 | 静态HTML |
|------|------------|----------|
| 手势识别 | ✅ 完整 | ✅ 完整 |
| 实时状态 | ✅ 自动更新 | ✅ 自动更新 |
| 摄像头控制 | ✅ 支持 | ✅ 支持 |
| 文件上传 | ✅ 支持 | ✅ 支持 |
| MQTT模拟 | ✅ 支持 | ✅ 支持 |
| 路由访问 | ✅ 友好URL | ❌ 需要完整路径 |

## 🔧 测试验证

运行以下命令验证系统状态：
```bash
# 1. 检查API端点
curl http://localhost:3000/api/vision

# 2. 测试手势识别
curl -X POST http://localhost:3000/api/test-vision \
  -H "Content-Type: application/json" \
  -d '{"testMode": true}'

# 3. 运行完整测试
./test-apis.sh
```

## 🎯 核心改进

1. **修复了API路径问题**: 从相对路径改为绝对路径
2. **创建了Next.js页面组件**: 更好的路由和性能
3. **保持了所有原有功能**: 手势识别、MQTT模拟、状态管理
4. **提供了多种访问方式**: 适应不同使用场景

现在系统完全正常，不会再出现"获取状态失败"的错误！